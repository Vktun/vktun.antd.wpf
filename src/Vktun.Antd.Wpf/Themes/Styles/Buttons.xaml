<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Vktun.Antd.Wpf">
    <Style x:Key="Antd.Button.BaseStyle" TargetType="Button">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource Antd.FocusVisualStyle}" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushText}}" />
        <Setter Property="Background" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushBgContainer}}" />
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushBorder}}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="16,0" />
        <Setter Property="MinHeight" Value="{DynamicResource {x:Static local:AntdResourceKeys.ControlHeightMiddle}}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource {x:Static local:AntdResourceKeys.BorderRadiusMiddle}}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushFillSecondary}}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushPrimaryHover}}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushFillTertiary}}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushPrimaryActive}}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55" />
                        </Trigger>
                        <Trigger Property="local:ButtonAssist.Type" Value="Primary">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushPrimary}}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushPrimary}}" />
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushWhite}}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="local:ButtonAssist.Type" Value="Primary" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushPrimaryHover}}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushPrimaryHover}}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="local:ButtonAssist.Type" Value="Primary" />
                                <Condition Property="IsPressed" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushPrimaryActive}}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushPrimaryActive}}" />
                        </MultiTrigger>
                        <Trigger Property="local:ButtonAssist.Type" Value="Dashed">
                            <Setter TargetName="RootBorder" Property="Background" Value="Transparent" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushPrimary}}" />
                        </Trigger>
                        <Trigger Property="local:ButtonAssist.Type" Value="Text">
                            <Setter TargetName="RootBorder" Property="Background" Value="Transparent" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="Transparent" />
                        </Trigger>
                        <Trigger Property="local:ButtonAssist.Type" Value="Link">
                            <Setter TargetName="RootBorder" Property="Background" Value="Transparent" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="Transparent" />
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushLink}}" />
                        </Trigger>
                        <Trigger Property="local:ControlAssist.Size" Value="Small">
                            <Setter Property="MinHeight" Value="{DynamicResource {x:Static local:AntdResourceKeys.ControlHeightSmall}}" />
                            <Setter Property="Padding" Value="12,0" />
                            <Setter Property="FontSize" Value="{DynamicResource {x:Static local:AntdResourceKeys.FontSizeSmall}}" />
                        </Trigger>
                        <Trigger Property="local:ControlAssist.Size" Value="Large">
                            <Setter Property="MinHeight" Value="{DynamicResource {x:Static local:AntdResourceKeys.ControlHeightLarge}}" />
                            <Setter Property="Padding" Value="20,0" />
                            <Setter Property="FontSize" Value="{DynamicResource {x:Static local:AntdResourceKeys.FontSizeLarge}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button" BasedOn="{StaticResource Antd.Button.BaseStyle}" />

    <Style TargetType="{x:Type local:IconButton}" BasedOn="{StaticResource Antd.Button.BaseStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:IconButton}">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource {x:Static local:AntdResourceKeys.BorderRadiusMiddle}}"
                            SnapsToDevicePixels="True">
                        <StackPanel Orientation="Horizontal"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                            <ContentPresenter x:Name="IconPresenter"
                                              Content="{TemplateBinding Icon}"
                                              VerticalAlignment="Center" />
                            <ContentPresenter x:Name="ContentPresenter"
                                              Margin="8,0,0,0"
                                              ContentSource="Content"
                                              VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Icon" Value="{x:Null}">
                            <Setter TargetName="IconPresenter" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="ContentPresenter" Property="Margin" Value="0" />
                        </Trigger>
                        <Trigger Property="Content" Value="{x:Null}">
                            <Setter TargetName="ContentPresenter" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushFillSecondary}}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushPrimaryHover}}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushFillTertiary}}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushPrimaryActive}}" />
                        </Trigger>
                        <Trigger Property="local:ButtonAssist.Type" Value="Primary">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushPrimary}}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushPrimary}}" />
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushWhite}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:Switch}">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource Antd.FocusVisualStyle}" />
        <Setter Property="Width" Value="52" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Switch}">
                    <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                        <Border x:Name="Track"
                                CornerRadius="14"
                                Background="{DynamicResource {x:Static local:AntdResourceKeys.BrushFillQuaternary}}" />
                        <Ellipse x:Name="Thumb"
                                 Width="22"
                                 Height="22"
                                 Margin="3"
                                 HorizontalAlignment="Left"
                                 Fill="{DynamicResource {x:Static local:AntdResourceKeys.BrushWhite}}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Track" Property="Background" Value="{DynamicResource {x:Static local:AntdResourceKeys.BrushPrimary}}" />
                            <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Track" Property="Opacity" Value="0.88" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
